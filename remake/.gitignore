# 遊戲資產(玩家自備原版後由 tools/ 產生)+ 建置產物,不入庫;引擎原始碼才入庫
# 用 assets/* 擋「內容」(map.json/units.json/sprites/tileset 等 ROM 衍生),但保留目錄例外能力
assets/*
# FIGANI 描述元（descriptor）+6 延遲表是小型、非像素的證據匯出；納入版控後，呈現橋接可在
# 沒有著作權原始封存檔的情況下重現。PNG 動畫幀仍維持忽略。
!assets/figani/
assets/figani/*
!assets/figani/delays.json
# 例外:原創劇本(可擴充事件 DSL 實例)入庫
!assets/scenarios/
# 例外:劇情對白文本(使用者確認 FD2 版權已過期,SRPG 對白允許公開;2026-07-03)
!assets/story/
# 例外：編輯器 canonical 文件只含可編輯結構、穩定身份、來源與原始索引；
# 不含 PNG／OGG／原始封存 bytes。二進位素材仍由下方全域規則與產生目錄排除。
!assets/editor-canonical/
!assets/editor-canonical/**/
!assets/editor-canonical/**/*.json
# 例外：官方／社群語言包契約與文字可版控；字型與圖像仍由下方全域資產規則忽略。
!assets/locales/
assets/locales/*/*
!assets/locales/*/pack.json
!assets/locales/*/content.json
!assets/locales/*/entities.json
# 例外：現代美術主題只版控來源、雜湊與成熟度目錄；原尺寸稿留在 generated-assets。
!assets/themes/
assets/themes/*/*
!assets/themes/modern/catalog.json
# 例外:法術表(EXE dump 數值,已在 docs/data 公開)
!assets/spells.json
# 例外:地圖的 RE 資料 JSON(units.json 單位數值/座標、map.json tile 索引/佈局)——
# 這些是我們反組譯自己產生的資料文檔,非版權(使用者 2026-07-04 明示)。
# 但同目錄的 tileset.png 是遊戲美術=版權,維持不入庫(下面 *.png 規則擋掉)。
!assets/maps/
assets/maps/*/*.png
assets/maps/*/*.ogg
# 例外:ending compositor 的反組譯 choreography JSON；不含原版圖像/字型。
!assets/endings/
web/assets/
web/fd2.wasm
*.wasm
fd2-linux
/fd2
fd2-verify
# 打包產物(AppImage/exe/dmg + linuxdeploy/appimagetool 快取):可重跑腳本重建,不入庫
packaging/dist/
