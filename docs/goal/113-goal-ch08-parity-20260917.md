# 113 — 第八章工作單元提示詞：原版一致對拍（王城前的戰鬥，map 7）（2026-09-17）

> 這是 [`111`](111-goal-original-parity-campaign-20260915.md) 的第八章工作單元開工指令。
> 接手時先讀 `CLAUDE.md`，再讀 `111`，再讀本檔；台帳
> [`parity-campaign-progress.json`](../data/parity-campaign-progress.json) 的第八章仍是 `todo`
> 才動工。本檔只定義這一章的順序、門檻與交付物；證據分級與位址規則仍以 `56`／`57`／`58`
> 為準，做完把結果寫回那三份與 `91-worklist-history.md`，本檔不承載結果。

第四到第七章已 passed（第七章 parity-ch07.json remake-r8／原版側 sample-r6，dosgolem `f57c23d`；
同一份重製端重跑第四／五／六章 remake-r14／r10／r10 仍過）。本階段只做第八章，順序固定，
做完才提交，不跨章。交付物與第七章相同：四 gate 收據＋抽樣截圖對照表。

## 這一章有什麼（開工前已知，動手時用收據核）

| 項目 | 內容 | 出處 |
|---|---|---|
| 節點 | 起點 `town_ch08`（祕密商店 selection 1、scan 90）→ `preparation_ch08` → `story_ch08`（raw `ch07_pre`）→ `battle_ch08`（map 7，29×45，目標敵全滅）→ `postbattle_ch08_persist`（raw `ch07_post`）→ `town_ch09`（祕密商店 selection 2、scan 101） | `remake/assets/scenarios/campaign_full.json` |
| 開場 runtime | 我方 10 人（slots 0..9）＋ group 0 的 19 筆（slots 10..28），共 29 筆；group 1、8、9、10 沒有產生端 | [`fd2_ch07_post_ida.txt`](../data/ida/fd2_ch07_post_ida.txt)（已證實） |
| slot 28 | `camp own`、raw `+6=2`、portrait 5，在 (14,26)，四周是敵人；戰後 `0x233C6` 把它放到 (14,16)，共同尾端 `JOIN5`（洛娜） | `map7_units.json`、`fd2_ch07_post_ida.txt` |
| 敵人 AI 起始 | slots 10..27 的 `+0x34` 是 `0x82` 或 `0x02`（低四位都是 2） | `map7_units.json` |
| 戰前 handler | `0x33219..0x3327D`：`0x205DA` → PAN(7,32) → ACTING 31 → text 0 → PAN(7,23) → ACTING 32 | `fd2_ch07_post_ida.txt`（已證實） |
| 回合事件 | 第 2～7 回合 event 27 `0x349D9`（以回合計數器 `[0x53BEF]` 當 group 呼叫 `0x10B4E`，每回合登場兩筆）；第 15 回合 event 28 `0x34A0E`（slots 10..27 `+0x34 &= 0x80`）；`turn_events.json` 標 `camp: enemy`，依第七章 event 25 的對照是 selector 0 | `turn_events.json`、`fd2_ch07_post_ida.txt` |
| 死亡程式 | `2:29`（型態 2、事件 29，`0x34A3C`，觸發單位 map 7 unit 0 ＝ slot 10 的 Lv15 騎士）：text 2 → slots 10..27 `+0x34 &= 0x80`（尾端共用 `0x34A19`） | `native_death_events.json`、`ch08.json` |
| 戰後 frontier | 29、31、33、35、37、39、41（依 event 27 觸發到第幾回合） | `fd2_ch07_post_ida.txt`（已證實） |
| 戰後 handler | `0x234BB`：佈位（10 格＋slot 28）、鏡頭 (8,14) → text 3 → ACTING 33 → text 4 → ACTING 34 → `0x11D40(0,255,64)` 立即全黑＋清 VGA → JOIN5 → `0x11506` → chapter=8 | `fd2_ch07_post_ida.txt`（已證實，E1） |

## 0. 回顧（半小時內，不改碼）

- 看 #34、#35、#38、#39 現況；第七章收據已加進 56 §111 五章回顧，這一步只確認沒有新留言
  或新形狀要併。下一次完整的五章回顧在第十章之後。
- 第七章 limitations 最後一條（重製端由建構槽 LOAD 進下一章時，JOIN 記錄殘值能否容忍）
  就是這一章要回答的：凱麗是第七章戰後 JOIN12、場上是友軍、沒被 `0x11506` 抄回，存檔裡
  x／y／`+0x28..+0x36` 都是殘值。建槽工具仍整筆覆寫，所以要用「第七章 remake-r8 寫出的
  酒店存檔」直接 LOAD 一次（步驟 2 的正對照），看重製端有沒有被殘值帶歪。

## 1. 先轉寫第八章的事件處理器（#33 的第八章份）

- event 27 `0x349D9..0x34A0E`：把 group 來源寫成 op（以 `[0x53BEF]` 當 group，不是固定
  group），確認有沒有 `[0x53AFA]` gate 或其他守衛；pan／acting／對白有就一起轉寫。
- event 28 `0x34A0E`：`ai_byte_and_range` 10..27、mask 0x80，已在死亡程式 29 的尾端證實；
  回合事件版本另外列一筆，兩者各自核對指令範圍。
- 轉寫進 `tools/extract_native_death_events.py` EVENTS（每條非序言指令都要被某個 op 認領），
  重生 `remake/assets/data/native_death_events.json`，
  `tools/sync_native_turn_events.py --write --chapters 4,5,6,7,8`。`ch08.json` 現有的
  `reinforce_ch08_e27_t2..t7` 是 spawn 版本，換成轉寫版本後 diff 要能對回每一個 op。
- 死亡程式 `2:29` 已在 `ch08.json`，核對消費端（`native_death_program.go`）還在、觸發單位確實是
  slot 10。
- 動手前查 `58` 與 `11-enemy-ai.md` 的 mode 2 語意（低四位 2 是什麼行為），寫進計畫開頭的註解；
  這決定敵人在騎士陣亡或第 15 回合之前會不會主動接戰，計畫怎麼推進全看它。

## 2. 建槽

`tools/fd2_chapter_slot.py build --base work/parity-state/ch02-cleared/FD2.SAV --target 7
--levels-per-chapter 6 --out-dir work/parity-slot-ch08/`。
正對照：與第七章 remake-r8 寫出的酒店存檔（`work/parity-slot-ch07/remake-r8/FD2.SAV`，與原版
sample-r6 整檔 sha256 相同，10 人含凱麗 char 12）逐 byte 比；差異只能是升級政策、擊殺經驗、
出售、瑪琳與貝克威的已知建構偏差，以及凱麗 JOIN 記錄的殘值（工具整筆覆寫、真實存檔是 LOAD 殘值），
其餘任何一個 byte 用 `tools/fd2save.py` 對回欄位再決定。
manifest 到 `docs/data/parity-slots/ch08-manifest.json`，合法性檢查
`docs/data/parity-plans/ch08-slot-load.jsonl`（LOAD → `town_ch08` → 五棟建築）。

## 3. 寫 `docs/data/parity-plans/ch08-sample.jsonl`

LOAD → 出口 YES → 戰前對白（`ch07_pre`：兩次 PAN、ACTING 31／32、text 0）→ 佈陣 10 格
（y 41..44，南邊）→ 第 1 回合起手與指令環 → 第 2～7 回合每回合 event 27 登場兩筆（至少推到第 7
回合，讓 frontier 走到 41）→ 我方移動＋攻擊、敵方回合 → 擊倒 slot 10 騎士觸發死亡程式 29
（text 2、AI byte 清）→ 接戰幾回合抽到清 AI byte 之後的敵方行為 → `force_enemy_clear`（只能在
抽完戰鬥節拍之後）→ 戰後 `ch07_post`（佈位、text 3／4、ACTING 33／34、全黑、JOIN5 洛娜）→
`town_ch09` 五棟 → 一買一賣 → 酒店存檔 → 祕密商店 chord（看 `town_ch09.native_secret_gate`）。

- **slot 28（洛娜）在敵陣中間開場。** 動手前先查 `0x205b4` 與 `ch07_post` 有沒有看它存活：
  陣亡是 game over、走另一個分支，還是不影響 JOIN5。會影響的話，第 1 回合先讓她往南脫離。
- 騎士 slot 10 在 (14,12)，離佈陣格三十列；擊倒它需要全隊北上。用第七章的 `move_unit`
  `toward`／`sweep_round`，不要派單一單位衝（第六、七章兩次教訓）。
- event 28 在第 15 回合。先報時長：推到第 15 回合的序列數 × 每序列秒數；如果騎士在第 15 回合之前
  就倒了，event 28 的 op 對同一批記錄是冪等的 `&= 0x80`，只要收據證明 slots 10..27 的 `+0x34`
  在第 15 回合沒有再變，就寫成「已由死亡程式 29 先觸發」；推不到第 15 回合就寫進 limitations，
  不硬推。
- 法術、物品、商店買入：驅動端到第七章都還沒有這三種指令。這一章先補「商店買入」
  （`town_shop_early_mid_late_e2` 的操作順序已在重製端，先 grep 再寫），補不起來就 limitations
  明寫；法術與物品維持 limitations。
- 事前算張數：變體只乘真的獨立變動的維度（56 §五章回顧）。

## 4. 原版側

```
FD2_ORACLE_STATE=<覆蓋層> FD2_ORACLE_FORCE_ENEMY_CLEAR=1 \
FD2_ORACLE_EIP_TRACE=0x13A9F,0x1E54A,0x4E893 \
tools/dosgolem_oracle.sh work/parity-slot-ch08/sample-rN docs/data/parity-plans/ch08-sample.jsonl
```

- `0x4E893` 追蹤是標配；敵方若放指令 1／2／3／6／7／8，先拿呼叫端序列再改重製端，不猜。
- 第七章 limitations 還欠兩條直接追蹤，這一章順便拿：戰鬥第一次重繪的 HUD anchor
  （`FD2_ORACLE_EIP_TRACE` 加 `0x1AD2A`）、mode 0 後備路徑的 `(start, budget, dest)`
  （加 `0x4E1A6,0x14B16`）。trace 會變大，先用一輪短計畫確認輸出量。
- dosgolem 缺口進 `~/cht/dosgolem-fd2-oracle` `feat/fd2-oracle-input-chain` 並提交，
  runner.json 不得 dirty；撞到第二個 opcode 就 capstone 掃整族一次補齊。
  開工時這個 worktree 領先 origin 1 個提交（`f57c23d`），push 前先問。
- 原版側跑時不要同時起 Go 全套件回歸（單一 replay 測試可以）。

## 5. 重製側

`tools/chapter_parity.sh 8 work/parity-slot-ch08 work/parity-slot-ch08/sample-rN
work/parity-slot-ch08/remake-rN`。

- `battle_ch08` 補 `native_map_view`（抄 battle_start 那張 checkpoint，不是戰中的）與 HUD；
  battle_start 的可見游標若落在 row>5 且 column<3 或 column>9，確認 anchor 規則有套到。
- `ch07_post` 的 `runtime_context.slot_counts` 改實跑 frontier，用收據核（29..41 中實際走到的那些，
  group 255 的 21 筆不算）。
- slot 28 是 `+6=2` 的 FDFIELD 我方記錄、不在持續隊伍裡：確認重製端的玩家操作、`0x13565`
  自動換手判定與 `0x11506` 戰後同步都把它算進去，JOIN5 之後酒店存檔 11 人。
- `ai_order` 分岔必須 0。
- 缺陷類 issue（#34、#35、#38、#39、#29、#32）不阻擋本章 passed：畫面差仍在 640 px 預算內就記進
  回顧表；有時間在步驟 7 之後挑一兩條做。#38 的 DAC 循環相位若讓重製端兩次重跑收據不同，
  先在 limitations 記下哪幾點、差幾步。

## 6. 抽樣截圖證據

`tools/parity_sample_sheet.py --receipt docs/data/ui-traces/parity-ch08.json --oracle
work/parity-slot-ch08/sample-rN --remake work/parity-slot-ch08/remake-rN --out
docs/figures/parity-ch08-samples.png --index docs/data/ui-traces/parity-ch08-samples.json
--include-seq <指定點>`（在 `fd2-assets-local` 容器內跑）。指定點至少包含：戰前 ACTING 31／32、
第一次 event 27 登場、死亡程式 29 對白、戰後全黑前一幀、JOIN5 之後的 `town_ch09`。超過 1.5 MB
依列數切張，不縮圖、不量化。57 的第八章段落貼圖與 index 連結；56 §第八章記用法。

## 7. 收尾

任一 gate 失敗：開 缺陷／RE待解 issue（`tools/fd2_worklist_issues.py new`），修掉重跑；
修不掉標 blocked 寫原因。全過才台帳第八章 passed（`tools/fd2_parity_progress.py verify`），
並用同一份重製端重跑第四～七章確認仍過。56 加第八章段、57／58 各一段、91-history 一則、
教訓寫成規則（`docs/data/fd2-lessons.json`），`#33` 留言記下第八章已轉寫的事件，
`fd2_worklist_issues.py pull` → `fd2_worklist.py verify` → render → commit。push 前問一次。
視時間 #39、#32、#29。

## 已知規則（沿用第七章，新增的放最後）

- 0x1A30B 順序：我方回復 → selector 1 事件 → `sub_1A866(1)` → 友軍 AI → 橫幅 → 0x13536 →
  selector 0 事件 → `sub_1A866(0)` → 敵軍兩遍；selector 2 事件與 `sub_1A866(2)` 在 PLAYER PHASE
  橫幅後、輸入前。
- 0x205b4 勝負只看已登場記錄；0x11506 照抄 +2/+3；存檔差 byte 用 fd2save.py 對回欄位；
  一章收據裡剛好相等的量不是規則。
- oracle_mid_end_turn 不比；checkpoint 落在 0x373C4／0x11EB0／0x4E809 內 dosgolem 已延後 PNG；
  idle 變體被 0x1297D 推進就重設再畫；敵方回合鏡頭是 0x12CEA 游標協定。
- MAP_LIMIT 63、stop_on_auto_end、清場後 await_ui；force_enemy_clear 只能在抽完戰鬥節拍之後。
- 第六章：佈陣格跟槽位不跟角色；HUD anchor 只在 0x1AD2A 真的畫時前進；ACTING 未登場但在 96 筆內
  是 no-op；待機前先重播游標鍵；battle_start 的 native_map_view 抄 battle_start；三個 selector 的
  暫時狀態掃描每回合都跑；指令演出擲骰與命中／傷害同一條 0x4E893 序列、未命中不抖動；
  重播在每個 0x13A9F 入口對齊亂數。
- 第七章：格子事件每一步都記、行動收尾才分派（`0x13565` 自動換手在 `0x1198A` 分派之前，AI 迴圈
  每筆記錄開頭寫回 0xff）；擊倒掉落是同步對話，型態 0 物品開框前放、型態 1 金錢關框後加；
  指令 13～16 回復量走 `0x4E893`；戰鬥第一次重繪就套 HUD anchor 規則；mode 0 的 `0x14121` 落點
  在原格就走 `0x13E9C`；`0x134E4` 在每個行動收尾把全部記錄 `+3` 歸零；升級同步 `+0x42`／`+0x46`；
  JOIN 記錄沒寫到的 byte 是 LOAD 時的殘值；候選狀態採用保留 Unit 指標（`adoptNativeStateCandidate`）；
  攻擊結果之後逐則按鍵（升級對話、掉落訊息）。

## 紀律

- 改過 Go 檔最後再跑 `tools/rebind_string_review.sh` → 複製 inv-new.json 成
  `docs/data/fd2-string-inventory.json`（gitignore，不進版控）→ 在 Docker 內
  `go run . -repo ../../.. -summary -output docs/data/fd2-string-inventory-summary.json` →
  `tools/migrate_full_locale_content.py`（新字串 `--new-translations`，machine_draft）→
  `tools/validate_full_locale_content.py`、`tools/validate_locale_packs.py
  remake/assets/locales/*/pack.json`、`tools/test_locale_review_blockers.py`。
- gofmt／go vet 在 Docker 跑；全套件 `tools/remake_go_test.sh <素材根> <log> ./...`；
  `tools/test_dosgolem_oracle_drive.py` 有 3 個既有 `/out` 權限錯誤（HEAD 也一樣）不算新紅。
- 提交前分開看每項檢查再 commit；push 前問一次；每批 Docker 後 `docker ps -a` 檢查 FD2 容器，
  禁止任何 prune／rmi；素材包有新增就同步 `~/cht/fd2-assets-private` 並跑
  `validate_separated_asset_pack.py`；不改 #15／#9／#6／#7／#8／#4／#10／#11。
