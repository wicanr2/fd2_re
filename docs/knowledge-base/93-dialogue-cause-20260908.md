# 王宮對話差異原因（2026-09-08）

2026-09-08 後續抽測勘誤：下述兩項失敗已修正測試資料與查詢方式，精確重跑均通過。
選人案例補齊原版 record 及合法六座標；開場多語案例分開姓名與對話 ID，
避免重複追加 `/text`，仍驗證全部條目的審核狀態與本文。翻譯本文無需修改。
補掛既有已驗證素材後，擴大抽測 37 項通過、3 項因晚期／第29戰外部存檔未提供而略過；
多語套件 20 項通過，均無失敗（計數包含子測試）。
[本次收據](../data/ui-traces/test-fixture-fix-20260908.json)保留命令與日誌雜湊；
舊 `pan-tests.log` 保留為失敗歷史。本次未改正式執行期，未重建或改寫下列 AppImage。

## 目前狀態：v.1.0.13-20260908（2026-09-08）

本機 [AppImage](../../dist-all/v.1.0.13-20260908/full/FD2-v.1.0.13-20260908-linux-x86_64-full.AppImage)
SHA-256 `ee12c87afc389ce7b1d549b2e8f3a52200f10fabb17eea55bc8342853e41aacc`。
[38 頁對照](../../dist-all/v.1.0.13-20260908/full/dialogue-review/index.html)與
[純雜湊收據](../data/ui-traces/dialogue-prefix-fix-20260908.json)可逐頁回查。

| 狀態 | 本輪結果 |
|---|---|
| remake 已修正 | 平滑移鏡終點的原生視圖同步；長句翻頁保留前文；等待翻頁箭頭；固定捲動矩形避免擦到頭像 |
| 正常玩家抽樣 | 實際 AppImage、全新設定、普通 START／Enter，38 張對話頁完成，無載入錯誤；通過舊包第30頁阻塞點 |
| 原版主要收據 | dosgolem 批次126自行執行65頁，300000000步預算停止，沒有新的不支援指令；不是第一關完整驗收 |
| 逐像素狀態 | 本次完整圖零差異的頁序為 [2, 4, 5, 6, 8, 9, 10, 11, 13, 20, 23, 24, 25, 27, 28, 30, 32, 34]；其他頁的未遮罩計數全部保留，沒有將文字差異算成動畫 |
| 已知 remake 缺口 | 原始字模347／584都被投影成「．」，反查只保留347，三點變單點；尚未修正無歧義字形投影 |
| 原版／背景待釐清 | 第一頁原有482框外差異中469已匹配5人物cycle0／2；其餘13在y21、x39及x256..267，不能說全屬動畫；裁切人物假說未命中，已排除該來源解釋 |
| 尚未提升 | 開場至第一關父子登場前全段操作一致、音訊／實體時鐘、亂數及逐幀動畫相位；不宣稱完整 PLAYER-E2 |

本切片的鏡頭交接、跨頁文字保留與捲動矩形為 RUNTIME-E1，加上正常
AppImage 抽樣收據；不是全段同狀態。針對性對話／來源對應／正式 START
回歸通過。較廣抽測的 `TestNativeContinueOpeningConfirmRejectsMovedCursor`
與 `TestReviewedOpeningTranslationsUseCanonicalLineIDs` 失敗保留於
本機 `pan-tests.log`，沒有把局部綠燈寫成全套通過。
本輪上述已修正的窄規格記為 CONFORMED；省略號字形與剩餘背景不在關閉範圍。

驗證過程保留：v.1.0.11 先閉合鏡頭阻塞；v.1.0.12 揭露原有捲動矩形
過寬，未作交付推薦；v.1.0.13 依 IDA 精確矩形修正。第一次 v.1.0.12
擷取因 Xvfb 尚未就緒而退出，已在腳本加入顯示器就緒檢查並乾淨重跑，
此為環境問題，不是遊戲崩潰。私人圖像及素材均未公開。


## 延長驗證勘誤：鏡頭終點交接（CONFORMED）

v.1.0.10 正常 START 擷取 29 張後，在 `0x3240B` 鏡頭拍後開框失敗。
原始開場 handler 記錄此拍呼叫 `0x135DD`，目的地為格 `(0,43)`；
既有綁定像素 `(0,1032)` 正確，但使用相容線性鏡頭路徑，完成時未呼叫
原生視圖同步。後續聚焦將舊視窗游標與新鏡頭混用，違反六個座標的同一狀態契約。
收據為本機 `work/dialogue-cause-20260908/remake-prefix-38/` 的 29 張圖與
`remake-state.jsonl`；這推翻「前兩句抽驗足以排除後續開場阻塞」的延伸解讀。

修正範圍：相容鏡頭抵達終點時，先走既有 `syncStoryNativeMapPanView`，
成功才執行後續拍；缺少合法格對齊仍拒絕，不猜補游標或略過檢查。
保留現有插值演出與原版逐格分支；此修正只閉合終點狀態交接，
不宣稱插值時序與原版一致。驗收涵蓋非零初始鏡頭、終點六座標恆等式、
後續聚焦與正常 START 重播。

## 延長驗證：跨頁保留前文（CONFORMED）

dosgolem `dialogue-prefix-126/frames/dialogue-003.png` 與 `004.png`
分別顯示父王長句的前三行，以及保留後兩行、在第三行接上「。』」的結果。
重製第 004 張清空前文，框內差 2,875 像素。既有 IDA 主證據
`fd2_dialogue_cause_20260908.json` 的 caller `0x15FC4..0x16044`
保留行號、等待後回到讀取下一 word，沒有整框清除；`0x16341` 的
`sub_16E24` 為既有三行窗口捲動 consumer。這足以修正跨頁資料流，
不需要新推測的文字分頁規則。

同一句的 typed pages 是輸入停頓點，不是獨立空白畫布。後頁應從前頁的
未加等待提示圖示之文字畫面接續；每新增超出三行窗口的行，捲動 19 像素。
逐字嘴型僅計算新頁普通字形，不能把重建前文或捲動畫格重算。
以既有十幀捲動近似發布，不重複施加舊的空頁翻頁延遲。
驗收需比較上下框跨頁保留區、終頁與 glyph timeline，並重跑普通 START。

另已證實原版等待翻頁顯示 FDOTHER #5 entry 18／19 箭頭；重製漏畫。
其定位受 `[0x53A51]` 分支影響，尚待核對 writer 後接入，不能僅憑一張圖猜補。

箭頭分支審查補記（READY）：既有 `0x108F0..0x10932`／`0x111BA`
writer 證據已在 `91` 的 composition storage 閉合索引：`[0x53A51]`
是已載入的 FDFIELD composition 指標，並非未知玩家選項。此處有原生地圖
載體，因此使用非零分支 `0x47A0`；無地圖分支不擴張本切片。
`0x16CD0..0x16CEB` 的 entry 18 目的地為文字基址加 `0x47A0+0x640`；
`0x16D21..0x16D4E` 每三次合格 tick 輪替 18／19。只在有後頁且已等待
輸入時呈現，按鍵後立即消失；仍採兩個重製更新對應一次合格 tick 的近似。

捲動矩形補證（READY）：v.1.0.12 實際跨頁已保留前文，但碰到頭像。
原來 renderer 用 control 的最大字數算寬度，錯把 FFED 的 15 glyph
資料上限當成捲動矩形。新 IDA 主證據 `fd2_dialogue_scroll_20260908.json`
保留固定 EXE 雜湊、IDA 9.4 LE 原位址與指令；逐項審查：
`0x16E69/0x16E8D` 以文字基址減一，對 72 列 memmove 208 bytes、來源高三列；
`0x16E9B..0x16EA9` 於文字基址加 `0x5A00` 清 208 bytes 為 `0x4A`。
外圈五次，再由 `0x16EBF..0x16EFF` 做相同四列位移與清理，共上移19像素。
以上矩形、次數與 consumer 為已證實，替代舊的「三列高度」推測。
正式 renderer 保留十幀節奏近似，但發布這六次原始複製操作的快照，
不再擦到頭像或框外；驗收包含上／下框與完整框外像素保存。

另找到小量文字差異的原因：原字模 347 與 584 在 `glyph_map.json`
都標為「．」，Unicode 反查只保留 347；原版 584 實際呈現三點。
此為來源身分遺失，非 dosgolem 文字解碼錯誤；待補無歧義字形投影，
本輪不以任意全域替換所有「．」掩蓋兩種原始字模。

## v.1.0.10 當輪結果：前兩句抽樣通過

本機修正版為 [v.1.0.10-20260908 AppImage](../../dist-all/v.1.0.10-20260908/full/FD2-v.1.0.10-20260908-linux-x86_64-full.AppImage)，
SHA-256 `aa9800de67385b25d3297af6d3b0a7483bbc70f55567b6de05adc5c2780df8a2`。
[本機對照頁](../../dist-all/v.1.0.10-20260908/full/dialogue-review/index.html)與
[可追溯收據](../data/ui-traces/dialogue-remake-fix-20260908.json)保存結果。

- 開框前的說話者聚焦、聚焦後背景保存與重繪已接回正常 START。
  父王第二句由 29,093 個不同像素降為完整 64,000 像素零差異；沒有遮罩。
- 逐字嘴型已接回：正常玩家 `first-flow-03.png` 的文字尚未寫完，完整
  80×80 頭像與 DATO resource 0 frame 1 零差異；其後亦錄到等待期 frame 3。
- 第一頁框內仍相符、框外仍差 482 像素；未鎖人物動畫相位，不宣稱全段逐幀一致。
- 11 項對話／正常入口／完整開場回歸通過，包含完整開場到第一關；GUI
  正常 START 本輪抽驗前兩句，不能把快速回歸當成新版完整開場 GUI 收據。
- 兩個缺口達 RUNTIME-E1，前兩句正常輸入抽樣已驗收；本切片規格
  CONFORMED。音訊、時鐘與第一關全段同狀態未提升。

第一次打包被素材清冊拒絕，原因是 `remake/generated-assets/` 的舊 HUD
圖塊雜湊與清冊不同；未跳過驗證。改由已驗證 v.1.0.9 AppImage 唯讀解出
完整素材，清冊通過後重建；新包亦從乾淨設定實際啟動成功。重播與收據工具為
`tools/replay_fd2_dialogue_review.py`、`tools/finalize_fd2_dialogue_review.py`；
原版圖與素材僅保留本機，未公開。所有本輪有界 Docker 容器已結束。

## 修正規格（2026-09-08，READY）

已審查 IDA LE `0x53A10/0x53A14` 載入映像均為零，直接交叉參照均在
`0x164E8`，沒有取址參照；caller 換句不重設。補出證據為
[計數器原始證據](../data/ida/fd2_dialogue_counter_20260908.json)。
以 Game 持續保存兩個計數器，每發布一個普通字形推進一次、每兩字切下一格，
值 3 映射為 frame 1。新框與等待輸入初始頭像為 0，但不重設累計；
分頁及換人不能改成固定從零起算。非繁中沿用已決定的不要求嘴型策略。
逐字更新與原版延遲的時鐘換算仍標為重製近似。

說話者依既有四控制碼的 raw identity／slot 契約解析；motion target 為零
保持不聚焦。命中時先以既有 X 先、Y 後、安全帶規則逐格聚焦；聚焦期間
不顯示／消費新句，完成後才合成背景、保存關框快照並開框。故事與戰鬥
兩種視圖載體均須同步，缺來源則拒絕，不加入章節座標常數。
驗收須包含跨句計數、未命中、聚焦中輸入、鏡頭與背景更新，並以新版正常
START 對照 dosgolem 前兩句。既有全開場回歸不得略過。

聚焦到父王後的回歸另揭露 HUD 前置驗證誤用：既有 `BlitNativeMapHUD`
契約在任一顯示閘門關閉時即返回、不讀角色資料，但 `nativeMapHUDInput`
仍驗證故事角色血量／職業，拒絕整張背景。依既有消費端契約同步提前返回；
不猜補父王戰鬥數值，正式顯示 HUD 的驗證保持原有要求。

以下保留修正前的原因查證：當時依使用者「先釐清差異原因」的要求，核對
dosgolem、DOSBox 輔助基準與實際 `v.1.0.9-20260907` AppImage。此處的
執行期缺口與待辦是當時狀態，已由檔首新版驗收結果取代。

## 修正前狀態（v.1.0.9）

| 項目 | 結果 | 分級 |
|---|---|---|
| 王宮第一句框內文字、頭像與框 | dosgolem 與 AppImage 完整框區零差異；框外 482 像素不同 | 局部畫面已證實，未鎖動畫相位 |
| 父王第二句框內文字、頭像與框 | dosgolem 與 AppImage 框區零差異 | 局部畫面已證實 |
| 父王第二句背景 | AppImage 框外 29,093 像素不同；原版開框前聚焦說話者，重製漏接 | RE-CLOSED；RUNTIME 缺口 |
| DOSBox／dosgolem 第一頁 1,298 像素差異 | 1,245 為人物畫格，53 為頭像畫格；沒有文字或框線解碼差異 | 來源匹配已證實；非完整機器同狀態 |
| 逐字期間頭像 | 原版有輪替；重製固定 frame 0，舊筆記斷言錯誤 | RE-CLOSED；RUNTIME 缺口 |

## 證據與版本

[原始指令與比較收據](../data/ida/fd2_dialogue_cause_20260908.json)保存固定版
`FD2.EXE` 的 SHA-256、大小、MD5、IDA Pro 9.4 版本與 LE 線性位址、原名、
bytes、交叉參照。原始匯出的 unknown 語意行僅供定位；本文件下列逐項審查
才是已證實語意，不把自訂名稱當作證據。

AppImage SHA-256 實際回讀為
`8fe7fccaa7af553c7fdba45c030223652c92cdb2307a61da962eb0d970bfdd4e`。
使用全新設定、原版主題、普通 START 與 Enter；只啟用唯讀輸入／劇情日誌。
音訊靜音，沒有直接進場、注入角色座標或改寫回合。

dosgolem 原版圖為其工作目錄的 `gap-122.png`／`gap-124.png`；DOSBox 的
`dialogue-window.png`／`dialogue-next-window.png`僅是輔助交叉驗證。
第二頁兩個原版執行器完整 64,000 像素零差異。AppImage 圖保存於本機
`work/dialogue-cause-20260908/remake-v4/`，含正常輸入重播與日誌，不公開原素材。
比較工具核對整數放大的每個像素區塊，不遮蔽無法解釋的像素。

## 已證實：開框前漏接說話者聚焦

IDA LE `0x165B9..0x165DE`：`sub_165AC` 的第三參數非零時，先將
`[0x51A83]` 設零，以前兩參數呼叫 `sub_12CEA`（`0x165D6`），之後設回一。
caller 提供說話者原始 record `+0/+1`；相機與游標沿既有安全帶規則更新。
`0x166E9..0x16712` 在此之後才保存 VGA 背景，故不得使用聚焦前的背景開框。

重製 `prepareNativeDialogueFrames` 直接以 `storyNativeMapView` 合成背景，
沒有此聚焦步驟；前兩句輸入日誌的鏡頭均為像素 `(72,480)`。
`resolveNativeStoryDialogueMotionTarget` 雖重建命中與 2／112／0，現有消費端
只用於收框尾段，沒有用來接開框前聚焦。這解釋父王第二句仍保留索爾視野。
本次只重開開框前置接線，不推翻既有五階段保存／還原框的證據。

## 已證實：逐字期間並非固定閉嘴

DOSBox 第一頁完整 80×80 頭像恰好匹配 `DATO.DAT` resource 0 frame 1；
dosgolem 恰好匹配 frame 0；鏡像錨點為 VGA `0x9017`，左上 `(8,115)`。
二者差 53 像素。地圖上索爾與四名衛兵的位置相同，DOSBox 是 FDICON cycle 1，
dosgolem 是 cycle 2；兩側每個不透明來源像素均匹配。下方兩名衛兵的來源辨識
只用未被框遮住部分；完整圖差異計數仍未遮蔽任何像素。

IDA LE `0x16497..0x164A7` 證實逐字延遲啟用時呼叫 `sub_164E8`。
`0x164F2..0x16532` 每兩次呼叫遞增 `[0x53A10]`，遇 4 歸零，值 3 映射為
frame 1，再於 `0x1652A` 呼叫 `sub_16559`。因此有 0、1、2、1 的輪替，
但本段不假設呼叫入口的計數器初值。等待輸入的 `sub_16C57` 另有 frame 0／3
狀態機，不能用該狀態機推論逐字期間沒有動畫。畫面上已出現所有文字，也不能
單獨證明 CPU 已進入等待輸入迴圈。

此直接 consumer 推翻 `fd2_story_dialogue_layout_ida.txt` 舊有「逐字期間
固定 frame 0」的推論；舊證據保留，需以本勘誤解讀。重製仍固定 frame 0，
應補接逐字畫格輪替；不需要為此深挖 DAC／PIT 音訊驅動。

## 修正前提出的切片（現已執行）

將開框前的說話者身分解析、聚焦、背景保存與開框接成同一條正常玩家路徑；
保留查找未命中時不聚焦的分支。逐字畫格應另外保存原版計數器初始化與重設
writer，再接既有頭像消費端，不能猜測每句重新從零開始。兩者修正後須重新
執行普通 START 擷取，不能將本次舊包截圖登記為修正版。精確時鐘、敵方亂數、
第一關全段與聲音仍未由本次局部查證提升。
