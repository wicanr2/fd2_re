# 101 — 回合橫幅的完整呈現（2026-09-09）

第 2 項回報「進入戰場後沒有 PLAYER PHASE／ENEMY PHASE 的動畫」。此前的紀錄
是「橫幅本身仍是重製端近似呈現，原版回合字樣未解出」。用逐幀擷取加
`-eip-watch` 一次量完，原版的呈現是**對已合成畫面做的 33 步對稱馬賽克**。

## 收據來源

原版執行器為 dosgolem `apps/fd2/cmd/oracle`（commit `f627cd1`），由
[`tools/dosgolem_oracle.sh`](../../tools/dosgolem_oracle.sh) 驅動。固定
`FD2.EXE`：357074 位元組，MD5 `b97caf2239a27a896069d03549d96e1e`，
SHA-256 `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`。
逐幀序列與雜湊見
[fd2-phase-banner-20260909.json](../data/ui-traces/fd2-phase-banner-20260909.json)。

入口是標題選單 `right,down,down,enter` 由附帶 `FD2.SAV` 進第一關，游標停在
空格按 Enter 開系統面板、`down` 到 END、Enter、Enter（YES）進敵方回合。輸入
全為一般 BIOS 按鍵，沒有狀態注入。

## 量到什麼

每張 320×200 索引畫面在地圖窗格 (4,4)-(316,196) 內取「同列連續同色長度」的
眾數，就是馬賽克方塊的邊長：

| 幀 | 0 | 1 | 2 | … | 15 | 16 | 17 | … | 30 | 31 | 32 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 邊長 | 1 | 2 | 3 | … | 16 | **17** | 16 | … | 3 | 2 | 1 |

33 張完全吻合 `邊長 = 1 + min(f, 32 - f)`。方塊由 1 漲到 17 再縮回 1，
對稱三角形。中位步距約 356,000 條指令，比一般重繪的 13 萬慢一些。

同一輪敵方回合結束後的 `PLAYER PHASE` 重現同一條曲線（frame 254..286），
兩者共用同一段排程。它在馬賽克開始前還有六幀維持銳利且不重繪。

`-eip-watch` 同時給出關鍵的一項：**整段橫幅期間地圖完全不重繪**。frame 0..31
的 `0x11CAC`／`0x122DC`／`0x11EEE`／`0x127A9`／`0x127E0`／`0x1AD72` 差值全部
為 0，frame 32 才恢復。畫面卻一直在變——所以馬賽克是對**已經合成好的畫面**
做的，不是重畫一張低解析地圖。

橫幅文字在整段期間保持銳利、置中，不隨馬賽克糊掉：地圖已經成了 17 px 的大
方塊時，`ENEMY PHASE` 仍是清晰字形。所以文字是在馬賽克之後才疊上去的。

## 兩項補充量測

**逐幀是精確的迴文。** 第 f 張與第 32-f 張畫面**逐位元組相同**，第 32 張與
第 0 張也相同（sha256 比對）。所以馬賽克不是逐步累積，而是每一步都從保留下來
的原始幀重算；只有 17 張不同的畫面，正播再倒播。

**取樣規則是方塊左上角。** 對邊長 4／8／16 逐方塊比對原始幀：左上角
1872/1872、468/468、114/114 全中，中心與右下角只有個位數百分比。格線原點就是
窗格原點 (4,4)。而且因為取的是原始像素本身，**整段沒有暗化**。

## 回頭對照既有 RE 文件

既有知識庫已經把這一段的**兩側邊界**釘住了，只是沒有人把中間那段畫面接起來。
[56](56-fd2-remake-sdd.md) 的 `sub_1A866` 狀態倒數一節列出三個 caller
（`0x1A4D1→1`、`0x1A55E→0`、`0x1A797→2`），交接檔另記 `0x1A7BD` 以
`0x111BA(0x1A4D,0,0x40)` 取得資源 handle、`0x1A7F1` 釋放。

以 `-eip-watch` 在同一輪量這五個位址，邊界完全對上：

| 取樣 | 事件 | 差值 |
|---|---|---|
| step 189512000 | `ENEMY PHASE` 起點 | `0x1A7BD` +1、`0x1A7F1` +1、`0x1A4D1` +1 |
| step 202264000 | `PLAYER PHASE` 起點 | `0x1A7BD` +1、`0x1A55E` +1、`0x111BA` +1 |

所以馬賽克那 33 步就發生在 `0x1A7BD` 取得資源與 `0x1A7F1` 釋放之間，而
selector 1／0 的狀態倒數各自掛在兩個 phase 的邊界上——這與 `56` 靜態解出的
順序一致，等於用執行期把那段靜態結論再驗一次。`0x111BA` 在其中一個邊界載入
一筆資源，是字模資產的追查起點。

一項不吻合要記下來：`0x1A797` 在 185M–208M 這整段**一次都沒有執行**，包含
玩家在 203.5M 之後重新取得操作權那一段。`56` 記的是「`0x1A797→2` 在 phase
畫面收束後設定 raw range mode 1、進入玩家輸入前」；敵方回合結束後回到玩家
輸入這條路徑顯然不經過它。這裡只記錄觀測，不改寫 `56` 的靜態結論——要改得
先確認 `0x1A797` 的正式進入條件。

## 重製端的接線

`indexedmap.MosaicNativeMapViewport` 依上述規則對保留幀重算，`PhaseBannerBlock`
提供 33 步的邊長曲線，`drawNativeMapFrame` 在橫幅期間改用馬賽克後的畫面，
`drawPhaseBanner` 只畫字樣、不再暗化地圖。

每步停留的幀數是重製端自訂的節奏：原版步距約 356,000 條指令，但虛擬時鐘是
每指令 1 微秒的近似，換不回實機毫秒。目前是每步 3 幀（33 步約 1.65 秒，與
先前 90 tick 相當）。形狀由收據釘住，時間不宣稱一致。

回歸 `TestPhaseBannerBlockIsTheMeasuredPalindrome` 與
`TestMosaicNativeMapViewportTakesBlockTopLeftAndKeepsTheBorder`。

## 尚未涵蓋

- 馬賽克與文字的繪製位址。本輪只量畫面，以及既有繪圖位址在這段的缺席。
- 字形尚未擷取成資產。原版用的是英文字樣，內容與重製端相同，但字模未比對；
  追查起點是 `0x1A7BD` 經 `0x111BA` 載入的那一筆資源。
- 每步的停留時間。收據只給指令數，換不回實機毫秒。
- 橫幅開始那一刻左下 HUD 面板在不在。本輪取到的原版那一段畫面沒有面板，但
  `0x1AD72`（HUD 組裝）在橫幅前一幀確實執行過一次（step 189376000），兩者
  還沒有調和。要注意這一輪的原版入口是「把游標推到空格→系統面板→END」，
  重製端目前用 Tab 快速鍵結束回合，兩條路徑進橫幅時的 HUD 狀態本來就不同。
  馬賽克本身對「當時合成出什麼」照樣套用，不受此影響。
- 馬賽克開始前的銳利保持幀數。`PLAYER PHASE` 觀察到六幀，`ENEMY PHASE`
  那一段的起點被取樣邊界切掉。
- 每步的實機毫秒。虛擬時鐘是每指令 1 微秒的近似。
