# 開場至第一關第五回合操作驗證

2026-09-08 延長對拍：v.1.0.13 已修鏡頭終點交接、跨頁保留前文、翻頁箭頭
與捲動矩形，實際 START 前38頁完成；dosgolem 原版收據延伸至65頁。
省略號字模碰撞與剩餘背景差異仍未關閉，全段一致未提升。
唯一目前結果與各頁未遮罩收據見 [93](93-dialogue-cause-20260908.md)。

2026-09-08 後續修正：dosgolem 已可自行產生王宮原版畫面；v.1.0.10 已修正
v.1.0.9 的第二句背景與逐字頭像接線，正常 START 第二句整張零差異。最新結果見
[對話差異原因](93-dialogue-cause-20260908.md)，不將下列較早抽樣提升為全段一致。

## 目前狀態（2026-09-07）

本次依使用者要求，以忠實原版主題的操作感覺為主，範圍為開場選單、完整
第 0 章、第一關至第五回合哈特父子出現前。檢查確認／取消、對話翻頁、
游標與角色移動、指令圖示、戰場 HUD、回合交接；敵方亂數只作對齊輔助。

| 對象 | 目前結果 | 證據／下一步 |
|---|---|---|
| 既有完整版 AppImage `v.1.0.3-20260907` | **不一致**：START 直接進入野外休息對話，略過王座廳 | 本機 `old-after-start.png`；原版相同 START 操作進王座廳 |
| 完整開場正常鍵盤路徑 | 實際完整 AppImage 已由 START 走完；97 句來源化回歸通過 | `audited-app.log`、`audited-walk-inputs.json`；不使用直接進場 |
| 王座廳第一句對話框 | 下框、左頭像、文字排版相符；已修深紅地形與索爾朝向 | `original-palace.png`／`audited-palace.png`；相近狀態，不宣稱逐像素一致 |
| 選人、移動、待機、取消 | 已接原生範圍／路徑、資訊框與游標復原，AppImage 一格移動後待機成功 | `audited-initial-range.png`／`audited-one-step-ring.png`／`audited-cancel-range.png` |
| HUD 與指令圖示 | 已修初始視圖與圖示展開／停留座標 | 原版普通 START 和新 AppImage 首次操作截圖；未鎖動畫相位 |
| 回合交接 | 來源身分接線已修；v.1.0.4 實際 AppImage 正常 END 到第二、第三回合，沒有滿血誤判敗退 | 舊測試人工補姓名已移除；全員不動在第三回合索爾 HP 歸零，該路徑停止，不反覆刷關 |
| 戰況資訊 | 已改正 codec 並重生；v.1.0.5／v.1.0.6 實際 AppImage 面板均正常 | 原版／最終包對照頁連結如下；開局金錢另由 v.1.0.6 移除猜測的 1000 |
| 本機交付 | `v.1.0.9-20260907` 本機候選；沿用原版完整分離素材，核對雜湊與擁有權 | 未公開發布，仍非完整逐像素一致或穩定版 |
| 原版父子登場時機 | DOSBox 合法 CONTINUE：TURN 003 的 END 後進入「老爸！老爸！」 | `oracle-turn3-info.png` → `oracle-end3-prompt.png` → `oracle-after-end3.png`；不能宣稱第五回合前仍未登場 |
| 中立輸入與狀態 | v.1.0.7 實際 START 已驗證 Escape 循環、取消優先、F2 兩頁狀態、已行動角色確認、跳過已行動者及回合聚焦 | 原版另以正常 CONTINUE 補拍；兩者非整幀同狀態 |
| 物品寶箱 | 已補第一關普通物品寶箱頭像問句、YES／NO、FFFC 藥草名稱及取得後確認 | v.1.0.9 實際驗證 NO 保留、下回合 YES；滿欄交換與其他寶物分支未提升 |
| 尚未關閉的證據 | 完整同狀態收據、精確按鍵重複／動畫相位／音訊 | 不把局部修正或綠色回歸稱為操作完全一致 |

交付：[`v.1.0.9 AppImage`](../../dist-all/v.1.0.9-20260907/full/FD2-v.1.0.9-20260907-linux-x86_64-full.AppImage)，
SHA-256 `8fe7fccaa7af553c7fdba45c030223652c92cdb2307a61da962eb0d970bfdd4e`。
本機[圖像對照與輸入收據](../../dist-all/v.1.0.9-20260907/full/input-audit/index.html)
保存原始截圖，不遮蔽差異；公開可保存的
[雜湊與方法摘要](../data/ui-traces/opening-input-audit-20260907.json)不包含原版素材。

版本範圍必須分開：v.1.0.7 由正常 START 播完完整序幕，實際選人／取消／
換人／F2 狀態／上移一格／待機／兩次 END 到 TURN 003，於父子登場前停止。
v.1.0.9 另補寶箱對話，完整開場回歸仍通過；實際包採合法原版 CONTINUE
驗證寶箱取消與接受、狀態欄和兩次 END，不把前一版序幕擷取改名成最終版。

## 方法與限制

- 原版為 `FD2.EXE`，357074 bytes，MD5
  `b97caf2239a27a896069d03549d96e1e`，SHA-256
  `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`。
- 本輪原版執行器為既有 `fd2-dosbox-screenshot-local` 映像中的 DOSBox，
  早期 `cycles=fixed 60000`、後續 `fixed 12000`，原始目錄唯讀，僅複本可寫；標記 `dosbox-bootstrap`。
  dosgolem 的 `origin/feat/fd2-parity` 場景明列相同輔助來源，不能將其包裝為
  dosgolem 自行執行或正式對拍完成。
- 重製直接執行 AppImage 的 `--appimage-extract-and-run`，使用全新 XDG
  設定與已分離原版素材。診斷啟用 `FD2_CUTSCENE_LOG=1` 與唯讀
  `FD2_INPUT_AUDIT_LOG`（逐次按鍵、回合、游標、角色狀態）；
  未使用直接進場、指定座標、強制回合或略過戰鬥。
- 本輪音訊關閉，結論限視覺與輸入，不證明音效或人耳同步。
- 原始操作截圖與重建候選暫存 `/tmp/fd2-feel-20260907/`，不是正式交付入口。
  完成時應整理可重現工具、必要收據及驗證後的交付物。
- 初次新版擷取腳本過早查詢視窗導致空識別碼等待，已分類為驗證環境問題；
  改以有界可見視窗就緒檢查重跑，不能記成產品缺陷。
- DOSBox 新 START 的長序幕與 CONTINUE 回合補測分別標記，未拼接成同一條
  連續紀錄。CONTINUE 使用固定原版目錄既有合法 `FD2.SAV` 的可寫複本，沒有
  修改亂數、座標、HP 或回合。前一次嘗試在容器到期前沒有完成 SAVE，不能
  宣稱已取得本輪 START 所寫的存檔。

## 故事地形初始化修正規格：READY

**已證實的既有契約：**`56-fd2-remake-sdd.md` 的原生地形與 steady-frame
條目，以及 `docs/data/ida/fd2_ch22_post_ida.txt` 的 `0x4DBFC` 複核，記錄
固定版本 FD2.EXE／IDA Pro 9.4 線性位址 `0x4DBFC..0x4DC34`：
每個執行期地形 cell 的 `+3` 設為 `0xff`。`0x11EEE` 的消費端只有在
`+3 != 0xff` 時套亮色 LUT。素材 JSON 內的 byte 只證明輸入形狀，不是
初始化後的活躍狀態。本次不重開該反組譯。

**已證實的重製缺陷：**`battle.LoadScenario` 已清為 `0xff`，但
`materializeNativeStoryMapState` 又把 `g.m.NativeTileBlitModes` 的原始值
複製到新的故事狀態。王座廳原版紅毯保持深紅，新版對話背景變粉紅，與此
錯誤資料流一致；原本的測試也錯誤要求原始值與執行期值相等。

修正只在建立故事 LOADCH 狀態時依既有 constructor 填入 `0xff`，
保持來源資料不可變、使用目前地圖的尺寸與元素數；後續正式動畫對私有
狀態的修改照常保留。拒絕不完整的素材形狀。存檔、規則與素材格式不變。
驗收為來源資料不變、故事地形旗標全部初始化、原生故事相關回歸通過，
並從重新編譯的 AppImage 普通 START 再擷取王座廳第一句與原版比較。

## 第一關初始游標修正規格：READY

原版普通 START 路徑在 `0x32961 → 0x12D7B(slot 0)` 後回到索爾，
既有 `docs/data/chapter_beats/ch00_pre.json` 保存該呼叫；完整開場回歸
也固定 slot 0 在 `(7,14)`。本次 DOSBox 戰場畫面中索爾位於可見格 `(7,1)`，
鏡頭 `(0,13)`。第一次確認操作因此作用於索爾，並可進入角色狀態面板。

`campaign_full.json` 的 `battle_ch01.native_map_view` 卻指定鏡頭 `(1,13)`、
游標 `(8,17)`／可見格 `(7,4)`，那是另一個既有戰場錨點；正式 START
不應被重設至該空地。只校正此節點既有六欄資料為上述原版初始視圖，
保留 `range_mode=1` 與 HUD 繼承契約。CONTINUE／存檔還原仍由存檔提供視圖，
不修改其他節點、資料格式或正式移動規則。

驗收在原有 97 句完整開場回歸末端增加「游標等於 slot 0 座標、鏡頭與
可見格等於原版錨點」，再由 AppImage START 路徑重驗首個確認選單。

## 故事人物方向保存規格：READY

背景修正後，王座廳第一句仍有索爾朝向差異：原版背向玩家，新版面向玩家。
既有 `0x32351 → 0x13185` 十五格上行證據及
`TestBeatScrollStep...` 回歸固定結束方向為 pose 2；本輪不是增加跪姿或新動畫。
直接程式追查發現故事第一次合成 indexed 背景時重新呼叫
`AppendNativeMapSelectorBatch → MaterializeNativeMapPresentation`，使已由
ACT／scroll_step 寫好的姿勢歸零。此處只是重建繪圖快取，不能再次執行
角色建構器的方向歸零。

故事快取建立改在私有角色複本預檢，成功後只發布 selector slot 對照；
保留原角色已存在的原生座標、方向、移動偏移及一般繪圖方向。
SPAWN 後尚未建立原生載體的角色，採已來源化的目前座標／方向建立，
非法方向或缺少 selector 來源則拒絕合成。新增回歸驗證快取重建不改寫
既有 ACT 結果，再由普通 START 截圖核對；不更改 `MaterializeNativeMapSelectorSlots`
對真正新角色的原始初始化契約。

同時讓 `stepStoryWalks` 在完成時將最終座標／方向／零移動偏移同步到原生
載體；舊程式只更新一般座標，靠上述錯誤重建間接補上座標，才掩蓋了漏寫。
原有十五格上行回歸增加原生 `(8,21,pose 2,motion 0)` 的驗證。

## 戰場操作繪圖追加規格：READY

乾淨完整 AppImage 普通 START 後，選索爾時錯誤退回 640×400 世界視野，
缺少上方資訊框；空地系統圖示也比 DOSBox 普通輸入的相同游標位置更擠。
這是重開本項的動態反證，不因舊矩陣的局部一致結論重做其他戰場功能。

2026-09-07 使用 IDA Pro 9.4、固定 SHA-256 的 FD2.EXE、LE 線性位址
複核 `0x1741c/0x176b4/0x179d5` 與 `0x117e7→0x18890`。
原始函式、bytes、交叉參照與逐項分級保存於
`../data/ida/fd2_opening_input_20260907.json`。

- **已證實：**`0x17587..0x175a7` 與 `0x177cb..0x177eb` 在每次繪圖
  **之前**更新四個位移；四幀公式應用 `(frame+1)*delta`，不是 `frame*delta`。
  停留由 `0x179d5` 使用 `[-0x23a0,0x378,0x3a8,0x2ac0]`，不可沿用動畫末幀。
- **已證實：**`0x18890` 使用角色 `+0x3b` 預算、`+0x20` 移動表；
  `0x1f183` 非零選表 19，否則 `+7==0x1c` 選表 16。
  玩家 caller 固定 selector 1，依 `0x145cd→0x4e040→0x146d1` 產生範圍；
  可重用已閉合的目的格核心，但不可沿用 AI 特例覆寫移動表。
- **已證實：**`0x18973→0x18b84` 在移動輸入前顯示 `0x18c6d` 資訊框。
  可視游標 column <7 時偏移 `0x984`，否則 `0x8ed`，stride `0x1c8`；
  換算 viewport 為 `(156,5)` 或 `(5,5)`，再加原生 present 邊框 `(4,4)`。
  姓名及 HP／MP／LV 沿用現有 `0x18c6d` 資產與字型核心；首次後續輸入
  清除此暫時資訊框。移動選擇持續使用原生視野與地形 LUT。
- **已證實：**取消移動範圍後 `0x1898d..0x189b2` 先清地形 byte+3，
  再回到原游標；確認目的地依已閉合的 `NativePathDirections(mode 0)` 產生路徑，
  不把舊版 BFS 路徑當成原版契約。行動後取消仍走原來的回復分支。

實作先預檢原生資料並在私有狀態計算範圍／路徑，成功才發布；非原生
手工測試場景保持原有路徑。驗收包括原版正常輸入範圍與圖示位置、取消不移動、
合法移動後待機、回合確認，以及原有 AI 範圍／路徑回歸不變。

實測追加：取消分支原本只寫正規化 `curX/curY`，下一次
`syncNativeMapView` 又從原生 view 蓋回舊值；依已證實 `0x12CEA` 的回復
目的格契約，須經正式 `MoveNativeMapCursor` 同步原生視圖，再清選取。
戰況資訊的 steady 也錯把「只有四個面板的工作 surface」當成完整畫面，
使戰場底圖消失；依既有 `fd2_nested_system_menu_ida.txt`／`sub_1B1E7`
保存戰場並分區複製的契約，steady 應把四塊最終位置貼回保存的 baseline。
這兩項採既有證據的 READY 接線修正，另加取消後同步與面板外像素保留回歸。

## 正常開場後錯誤敗退修正規格：READY

實際 AppImage 第一回合 END 後，索爾 HP 42、在場，卻由下一次確認進入
`retreat_ch01`。唯讀觀察紀錄顯示原生開場角色的 `Name` 為空，而
`Game.checkResult → State.Result` 仍以 `Name == "索爾"` 判斷存活。
這是重製內部身分接線缺陷，不是原版死亡條件的新推論。
既有完整開場測試在勝利檢查前人工補 `Name`，因而掩蓋此問題，須移除。

沿用既有可編輯 `protect` 來源姓名契約；有原始 record +8 的角色透過已審查
`locales/zh-Hant/entities.json` 的 `battle_names` 對回來源姓名，與目前
顯示語言無關。舊來源化角色（本次 START 即如此）只有 `native_identity`，
使用同一目錄的 `characters` 對照，不假稱已保存 runtime +8。
手工場景沒有原版身分時仍採原有姓名比對。不得修改角色
顯示名稱、猜測新身分或在缺少來源目錄時繼續判定；載入錯誤應明示。
這不把 `0x205b4` 的數值結果自行升格成新勝敗語意，也不改其他章節規則。
驗收須在未補姓名的完整開場測試確認不敗退，再驗證目標死亡會敗退；
冷存檔還原與不同官方顯示語系保持同一結果，最後以 AppImage END 重跑。

完成玩家行動時須在釋放選取前清掉本次移動預覽；否則下一回合仍殘留
舊移動取消狀態。此為本輪新增狀態的生命週期修正，加入完成回呼前清理測試。

## 戰況面板圖塊解碼勘誤：READY

v.1.0.4 實際系統資訊仍破圖；直接檢視分離包 entry 134 PNG，斜線已存在於
素材。IDA Pro 9.4 複核固定版 `0x1b41d`（`ida-info-codec.json`）證實四次
繪圖呼叫都是 `sub_16886`（`0x1b447/0x1b464/0x1b489/0x1b4ac`）。既有
`56` 的 `0x187d6→0x16886→0x4e63d` 證據已確立四模式解碼器，並不是
`0x4e916` 高位重複解碼器。

這推翻 `fd2_nested_system_menu_ida.txt`「四圖塊沿用 DecodeLMI1Resource」
的舊接線斷言：當時測試只比同一錯誤解碼器，不能證明原版畫面一致。
只將 #5 entries 133–136 移至既有 `four_mode_frame` 型別，保留索引、尺寸、
透明遮罩與原版字型／數字；loader 拒絕舊 codec metadata，匯出工具重生
PNG／mask。其他 opaque entries 20–22 不變。既有格式已支援四模式，
不新增格式或猜測新解碼器。驗收以原版戰況截圖核對面板、四個數值區與背景，
並更正 archive 解析測試為真正 `ParseLMI1FrameEntry` 路徑。

## 開局金錢勘誤：READY

原版正常 START 後的 `original-check-turn2-info.png` 顯示 `$00000000`；
未擊倒敵人、未購物的第一關路徑沒有金錢交易。新包顯示 `$00001000`，
直接程式來源為頂層 `loadGame` 建構器的 `g.gold = 1000`，其舊註解也明寫
「原版開局金額待對照」。既有金錢顯示 consumer 已閉合，本次不重解計數器。
移除這項早期猜測，普通新遊戲初始化為已觀測的零；存檔載入與後續交易
仍由各自 owner 還原或更新，不改資料格式。原版金額為視覺已證實，
不宣稱本次已追到原版初始化 writer 的每條指令。

## 額外發現與驗收限制

v.1.0.4 的 F5 已在第一回合正常寫出 `fd2_save.json`，但後續 F9 補測把
索爾 `(7,14)` 還原成 `(7,20)`，其他隊員也出現座標差異；沒有改寫存檔。
此補測已停止，不能把「檔案存在」稱為冷讀檔通過。這是另一個尚未關閉的
執行期接線問題，來源 writer／restore owner 尚待定位；本次 START 比較
不以此狀態繼續或假造到達登場邊界。

**同輪勘誤：**回讀 `56` 的巢狀系統存檔契約後，確認 F5/F9 的 JSON 保存
本來就是 battle-node restart，與原版 current-runtime snapshot 不同。
上述座標差異不能直接定性為本輪新引入的資料毀損，也不應另開猜測修正；
它只證明這組重製快捷鍵不能代替原版 SAVE／CONTINUE 作同狀態續玩驗證。
最終包使用真正原版合法存檔的 CONTINUE 已成功還原原視圖。

## 中立輸入與回合聚焦補正：READY

2026-09-07 追加窄證據：`fd2_player_neutral_input_20260907.json` 由相同
FD2.EXE 雜湊、IDA Pro 9.4 LE 線性位址重生，保留 bytes／原名與交叉參照。
以下語意已經直接指令審查；原始匯出未分級行仍不得單獨視為語意證明。

- `0x10602` 初始化 `[0x53AE9]=0`；`0x1180D..0x11878` 從該索引環狀掃描
  raw records，選第一個 `+5 & 0x85 == 0` 且 `+6==2` 的角色；呼叫
  `0x12D7B(index)`，索引更新為下一筆，達 count 時歸零。沒有候選時不變。
  入口掃描碼為 `1/0x2C/0x4C`，本次接 Escape／Z／數字鍵盤5；Backspace
  仍是重製取消別名，不新增為原版循環選人。選取中 Escape 的取消層保持原契約。
- `0x1A781` 每回合把該索引清零，`0x1A79F..0x1A7B0` 設 range mode 1
  後以 raw index 0 聚焦。`0x12D7B` 讀角色 `+0/+1` 交給既有 X 先、Y 後
  `0x12CEA` 與安全帶捲圖。本次每個更新畫格走一格，為可重現近似，
  不宣稱 DOS 時鐘一致；過程阻擋其他玩家輸入。
- `0x118FC/0x11909` 拒絕 raw `+7==0x79` 或 `+0x1F==10`；其餘角色若
  raw camp 非2、`+5 bit7` 已設或 `+0x26!=0`，確認鍵進 `0x17AED`。
  `0x119D2..0x11A17` 亦允許 F2／Home 直接查看同樣有效的角色。
  重用已驗證的 item/status renderer 與 `0x18409` 十二幀開關框；
  有 command mask 時，任意既有確認／取消鍵依序進命令頁再關閉。
  戰場獨立保存背景與返回狀態，不借用教堂 roster，也不改角色或存檔。

既有 `0x13512` 成功收尾／`0x13536` 全體清除 raw bit7 的契約仍沿用
`native_record_flags.go` 與 `56` 主證據；本輪 `0x1A543/0x1A5FA` 再確認
新回合呼叫清除者。原生玩家移動成功待機時必須同時發布 raw bit7 與既有
typed Acted；下一回合既有 Acted 清除處同步 raw bit7，不能讓兩個消費端分裂。
實際原地待機會在指令環前釋放移動預覽，因此成功收尾必須依原生戰場與
選取角色的 provenance 同步 bit7，不能以預覽仍存在為條件。v.1.0.8 寶箱
第二回合補測揭露此分支，v.1.0.9 修正並加入無預覽的原地待機回歸。

驗收：循環順序與排除條件、無候選、取消優先、聚焦安全帶／新回合索引、
狀態兩頁與缺資產失敗即關閉，最後以實際 AppImage 普通 CONTINUE 重跑。

## 第一關物品寶箱對話補正：READY

實際普通移動 `(7,14)→(7,13)` 後待機，原版 `0x190AC` 出現索爾頭像、
FDTXT `0x1A5` 問句與 YES／NO，接受後續寫 `0x1A6` 並把 FFFC 換成
`item+0xB5` 的名稱，等待確認後收合。v.1.0.7 實際只顯示 `取得物品 C0h`，
因此這是正常玩家路徑矛盾，不是素材或存檔缺檔。原版圖為本輪
`oracle-neutral-acted-status.png`（檔名是先前預想的操作；實際為寶箱回覆）。

窄主證據 `fd2_treasure_input_20260907.json`：相同 FD2.EXE 雜湊、IDA Pro 9.4
LE 線性位址，`0x19118→0x1956B` 消費 raw +7 頭像；`0x19125..0x1917E`
依可見／隱藏 flags 選 `0x1A5/0x1AC` 並呼叫既有 `0x19953`。
`0x1918A/0x19193` 僅接受結果1且選項0；`0x191DD..0x19234` 設名稱索引
並畫 `0x1A6/0x1AD`；`0x19237..0x1926D` 嘗試加物品、成功標記開啟、
`0x16559/0x16C57` 等待後收框。取消 `0x19523..0x19564` 顯示 `0x19C`，
不改寶箱／庫存。分離 FDTXT_000 JSON 直接保留 FFFC 詞元。

本次範圍僅「有空物品格的普通物品寶箱」及其取消：共用原版問句框、
選擇框、逐字回覆與人物頭像。預先驗證完整素材／可領取條件，YES 回覆
完成後發布既有 ClaimTreasure 交易，等待確認才完成行動；NO 不領取。
這個發布點是明示的畫格近似，不宣稱與原版每條 VGA 寫入同時。
滿物品格的交換流程、金錢與特殊事件分支不以本次證據提升，仍沿用原有
執行期範圍與限制。驗收包含 NO 不領取、YES 只領取一次、取消後同回合
行動結束、普通待機無寶箱不受影響，以及實際 AppImage 正常移動後完整問答。

## 最後驗證與清理

- Docker `fd2-go-test-local`：`internal/battle` 與 `internal/fdother` 全套通過；
  系統資訊、四語系來源身分、完整 97 句開場、移動／取消與正式 START 金錢
  聚焦回歸通過。原始命令與日誌保存於上述本機收據。
- 不宣稱整個 `cmd/fd2` 套件全綠。早期全套有 33 項失敗，其中 5 項以
  `2a4aab38` 原始碼覆蓋層重現相同失敗；不能將未逐一核實的其餘失敗
  一律稱為既有。`internal/indexedmap` 另有保存用素材包與固定 archive 的 HUD
  比較失敗，未用它替代實際完整包的觀察。
- 正確來源包由 `fd2-asset-import -fdother ... -out ...` 重生，
  `generate_separated_asset_manifest.py`／`validate_separated_asset_pack.py`
  通過；resource 覆蓋不變，canonical summary 只更新 manifest SHA-256。
- 最終 AppImage 實際二進位 SHA-256 為
  `ff01164fdab4d67bfa1c2077086aab63b3bb02fb2f7d7c501409b8afc51a31b2`，
  從執行中的 `/proc/.../exe` 回讀。entries 133–136 的 codec／mask 於 v.1.0.6
  實際解包核對，後續沿用同一已驗證 manifest 的完整素材重建。
- 本輪所有 `fd2-feel-*` 容器已結束並移除；未建立新工具映像。
  未提交／推送原始碼，也未上傳任何含原版素材的封包。
